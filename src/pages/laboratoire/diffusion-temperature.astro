---
import LabAppLayout from "../../components/laboratoire/LabAppLayout.astro";
import DiffusionTemperatureSimulator from "../../components/laboratoire/DiffusionTemperatureSimulator.astro";
import { labApps } from "../../data/laboratoire/apps";

const app = labApps.find((item) => item.slug === "diffusion-temperature")!;
---

<LabAppLayout
  slug={app.slug}
  title={app.title}
  description={app.objective}
  icon={app.icon}
  level={app.level}
  concept={app.concept}
  objective={app.objective}
  legacyPath={app.legacyPath}
>
  <DiffusionTemperatureSimulator />
</LabAppLayout>
